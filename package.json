{
  "name": "redflag-check",
  "version": "0.1.0",
  "description": "Red Flag Warning zone check, address-in, plain-English-out. Public API + web UI.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:vercel": "vercel dev",
    "deploy": "vercel --prod",
    "test": "bun test",
    "test:live": "RUN_LIVE_RFW=1 bun test live-rfw-geographic",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^5.1.0",
    "typescript": "^5.3.0"
  }
}
